# ideate-core-evals — status for Thursday (as of 2026-09-07)

## One-line answer

The **harness** is essentially done and behaves correctly under stress. The **study** is not: 2 of 9 arms and 6 of 24 briefs have been touched, and **0 of 5 registered hypotheses are estimable** from the data on disk. What you have is a working instrument plus a smoke run — not results.

## Phase ledger (against PREREGISTRATION §8.3)

| Phase | State |
|---|---|
| Phase 0 — negative controls + DAT replication | ✅ run live against Voyage-4-lite; 3 controls passed |
| Phase 2a — ~20-run smoke (#8) | ⚠️ **ran, not signed off**: `results/`, 20 cells, arms A & B × 2 briefs × 5 reps. #8 is still open (`~operator`/`needs-decision`) |
| Phase 2 — the pilot (#49) | ⚠️ partial: `results-pilot/`, arm B × 4 briefs × 4 reps (16 cells) + arm D 0/3 (all failed). Has NOT produced the replicate count it exists to determine. |
| Phase 3 — confirmatory grid (#50) | ❌ not started |
| Judge validation gate (#16) | ❌ not cleared — blocked on Si et al. expert-score access (`needs:host-read`) |

## Is the data clean?

Yes — and unusually so. Every check I ran came back consistent.

- **Accounting reconciles.** 139 rows in `results/`, 59 in `results-pilot/`; every cell in exactly one terminal state. No unaccounted cells.
- **Stale data is quarantined, not pooled.** 119 cells under the superseded configHash `5ce5478956e5` are excluded by the analysis run and reported as excluded. The 20 `__pruned-cell__` rows are spend-salvage records from that generation (issue #98), correctly `failed` and out of frame.
- **Judge scores attach to the live config.** 28 of 40 judge-score rows in `results/` carry the current hash `0000daa58e5a`; all 14 in the pilot do.
- **Engine pin is current.** Evals pin `ideate-core@0.4.0`; ideate-core HEAD is 0.4.0. The only post-collection commit touching `lib/` (425ad62) is **comment-only** — verified by diff. No cell is stale by engine drift.
- **Test suite green:** 1043 pass, 0 fail, 1 skipped.
- **Arm D's 3/3 failure is a fact about the night, not a live blocker.** All three failed 2026-09-03 09:43–09:53Z with truncated Opus pools; the #122 fix (per-model `max_tokens`) merged 2026-09-03 17:10Z — *after*. Arm D is re-runnable.

The one thing that is **not** backed up: both `results/` (gitignored) and `results-pilot/` (untracked) exist only on this laptop.

## The conclusion you can draw — and the one you cannot

The registered analysis CLI runs clean on `results/` and returns:

> **REGISTERED FAMILY NOT ESTIMABLE FROM THIS ARM SUBSET** — 5 of 5 hypotheses cannot be estimated.

That refusal is the honest headline, and it is the harness working as designed (it declines to substitute a present arm for an absent one).

### The finding that matters, and it cuts against the product

Full-pool `distinct_k` looks like the panel wins:

| Arm | Mean distinct_k | Candidates | Mean cost |
|---|---|---|---|
| A (solo Sonnet) | 27.10 | 30 | $0.0397 |
| B (5× Haiku panel) | 30.40 | 60 | $0.0382 |

**But B got there with twice the candidates.** Rarefying both arms to a matched 27 candidates (repo's own `rarefaction.mjs`, r=1000, pinned seed, registered threshold):

| Arm | Full-pool | **Rarefied to 27** |
|---|---|---|
| A (solo Sonnet) | 27.10 | **24.57** (sd 1.65) |
| B (5× Haiku panel) | 30.40 | **17.13** (sd 4.45) |

The ordering **reverses**. At equal volume the single Sonnet call is markedly more diverse than the five-Haiku panel, and far more consistent. B's full-pool edge is bought entirely with volume, at roughly equal cost.

### The rounds confound — checked and eliminated

Arm A is one round (30 candidates); arm B is two rounds that *append* (30 + 30 = 60), with round 2 run under pool-sharing. So B's rarefied draw was half round-2 output produced with sight of round 1 — which would mechanically depress its diversity. Restricting **both arms to round-1 candidates only** and rarefying to 27 (pool arrays verified index-aligned to `candidates`, A `roundsRun`=1, B `roundsRun`=2):

| Arm | Rarefied to 27, **round 1 only** |
|---|---|
| A (solo Sonnet, 30 cand) | **24.57** (sd 1.65) |
| B (5× Haiku panel, 30 cand) | **16.42** (sd 4.18) |

The gap is unchanged. **The reversal is not a rounds artifact** — it holds in a clean like-for-like comparison: same volume, same round count, panel vs solo.

A secondary finding falls out of this: round 2 doubled B's candidates (30 → 60) and moved rarefied `distinct_k` only 16.42 → 17.13. **The second round bought volume, not variety.**

**What this still does NOT show:** arm A is Sonnet and arm B is Haiku, so panel-vs-solo remains confounded with Sonnet-vs-Haiku. This cannot distinguish "the panel doesn't help" from "Haiku is less diverse than Sonnet." **Arm C (homogeneous Sonnet panel) is the cell that separates them, and it has not been run.** Do not state this as a refutation of the multi-agent claim on Thursday — state it as a signal that makes running C urgent.

## Limitations to say out loud

1. **Brief variance swamps arm variance.** Arm B: biz-01 40.4 vs biz-02 20.4. Pilot: sci-03 48.8 vs biz-04 25.5. The brief matters roughly twice as much as the arm across everything measured.
2. **n=2 briefs in the main store cannot support brief-as-random-effect.** The mixed model fit at rung R0 but the CR2 robustness check refused outright: *"fewer clusters (2, brief) than parameters (2)."* Any CI from this store is not trustworthy.
3. **Idea-level metrics are exploratory by construction** (Appendix B item 7) because judge validation (#16) has not cleared — regardless of the judge rows on disk.
4. **H5 (self-preference bias) is structurally unidentifiable** without arm G — the bias column is collinear with the judge-provider dummy when every generator is Anthropic.
5. **Cost separation is absent.** distinct_k/dollar: A 683 [657, 713], B 796 [534, 1058]. Overlapping — no ranking is supported. (These are full-pool figures; on a rarefied basis A's cost-efficiency advantage would widen.)
6. **Main-store and pilot dollars are not comparable and are deliberately not combined here.** `results/` rows are `billing_mode: "api"`; the pilot ran through the batch provider at a different pricing regime.

## What I'd ask for on Thursday

Not "here are the results." Rather: the instrument is built, validated, and has already produced one counterintuitive signal that survived a confound check — fund the confirmatory grid (~$79, ~$160 with contingency) to find out whether it holds.

Three concrete asks, in order:

1. **Sign off on the Phase 2a smoke (#8).** It is still open, labeled `~operator`/`needs-decision`. Phase 2 does not proceed without that decision — this is an unblock, not a spend.
2. **Run arm C (homogeneous Sonnet panel).** The single highest-value cell: it separates "panel doesn't help" from "Haiku is less diverse," which is the product's core claim.
3. **Run arm A′ (persona-disabled Haiku panel) against B.** The clean persona ablation — same model, same panel size, personas on/off. Cheap, and isolates the one thing arm C cannot.

Also worth flagging: **both result stores exist only on this laptop.** Back them up before Thursday.

---
*Generated from `results/` (cfg `0000daa58e5a`) via `evals/analysis/analysis.mjs`; dollars priced at read time from `lib/price.mjs`'s pinned rate table.*
