# Pinned toolchain for evals/analysis/sidecar/fit_mixedlm.py (issue #46).
# Installed into an in-repo, gitignored venv at evals/analysis/sidecar/.venv —
# no global installs, nothing outside the repo. `analysisHash` (fit.mjs) is
# computed from the toolchain versions the sidecar reports at runtime plus
# sha256(fit_mixedlm.py), so bumping a pin here changes analysisHash but
# never lib/manifest.mjs's configHash — see fit.mjs's header.
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3
statsmodels==0.15.0
